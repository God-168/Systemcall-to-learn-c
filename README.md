# Systemcall-to-learn-c
You can be a real coder who are not realy on standerd library.And you can just use the API which provied by  system kernel .

How to compile it ?

1 step:
On Linux,you can use command       gcc hello.c        to compile it .

2th step:
And compiler will show you a file which named a.out ,you can use command     ./a.out      to run it.
